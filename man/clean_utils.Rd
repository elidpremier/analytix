% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_utils.R
\name{clean_utils}
\alias{clean_utils}
\alias{clean_text}
\alias{clean_binary}
\alias{clean_numeric}
\alias{clean_names}
\alias{clean_column_names}
\alias{impute_mode}
\alias{impute_mean}
\title{Utilitaires de nettoyage, d'imputation et de normalisation de noms}
\usage{
clean_text(x)

clean_binary(x, yes_label = "Oui", no_label = "Non")

clean_numeric(x)

clean_names(x, max_length = NULL, prefix = "v_")

clean_column_names(x, ...)

impute_mode(x)

impute_mean(x, type = c("mean", "median"))
}
\arguments{
\item{x}{Vecteur ou data.frame à nettoyer ou imputer.}

\item{yes_label}{Libellé pour la valeur positive des variables binaires (défaut: "Oui").}

\item{no_label}{Libellé pour la valeur négative des variables binaires (défaut: "Non").}

\item{max_length}{Longueur maximale optionnelle pour les noms de colonnes.}

\item{prefix}{Préfixe à ajouter si le nom commence par un chiffre (défaut: "v_").}

\item{...}{Arguments supplémentaires passés à \code{clean_names}.}

\item{type}{Type d'imputation pour les variables numériques: "mean" (moyenne) ou "median" (médiane).}
}
\value{
Un vecteur ou data.frame nettoyé ou imputé.
}
\description{
Fonctions d'aide pour le nettoyage des vecteurs texte/binaires/numériques,
la normalisation des noms de colonnes (\code{clean_names} / \code{clean_column_names}),
et l'imputation par le mode ou la moyenne/médiane.
}
\examples{
clean_text(c("  homme ", "", "NA", "femme"))
clean_binary(c("oui", "NON", "1", "0", "Yes"))
clean_numeric(c("12,5", " 15 ", "NA"))
clean_names("Âge/Ans")
clean_column_names(data.frame("Nom Patient" = 1:2))
impute_mode(c("A", "A", "B", NA))
impute_mean(c(10, 20, NA, 30))

}
