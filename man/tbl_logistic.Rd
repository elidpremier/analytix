% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epid_stats.R
\name{tbl_logistic}
\alias{tbl_logistic}
\title{Tableau formaté d'une régression logistique multivariée}
\usage{
tbl_logistic(
  model,
  data = NULL,
  var_labels = NULL,
  conf_level = 0.95,
  digits = 2,
  color = "transparent"
)
}
\arguments{
\item{model}{Objet \code{glm} (famille \code{binomial}) ou formule R.}

\item{data}{data.frame (requis si \code{model} est une formule).}

\item{var_labels}{Vecteur nommé de libellés pour les variables explicatives.}

\item{conf_level}{Niveau de confiance (défaut: 0.95).}

\item{digits}{Nombre de décimales pour les OR (défaut: 2).}

\item{color}{Couleur d'en-tête du flextable (défaut: "transparent").}
}
\value{
Un objet \code{flextable}.
}
\description{
À partir d'un objet \code{glm} ou d'une formule + données, génère
un tableau \code{flextable} de la régression logistique multivariée (ORa, IC95\%, p-value).
}
\examples{
mod <- glm(am ~ cyl + wt + hp, data = mtcars, family = binomial())
tbl_logistic(mod)

}
