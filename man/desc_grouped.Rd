% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descr_grouped_categories.R
\name{desc_grouped}
\alias{desc_grouped}
\title{Description des catégories groupées (ex: molécules par classe thérapeutique)}
\usage{
desc_grouped(
  data,
  cols = NULL,
  group_col = NULL,
  sub_col = NULL,
  var_labels = NULL,
  pct_type = c("total", "group"),
  caption = NULL,
  digits = 1,
  color = "transparent"
)
}
\arguments{
\item{data}{data.frame contenant les données.}

\item{cols}{Vecteur de noms de colonnes représentant chaque groupe en format large (ex: \code{c("diuretique", "iec", "ara2")}).
Chaque colonne contient les éléments/modalités (ex: "Amlodipine").}

\item{group_col}{Nom de la colonne contenant la catégorie parente en format long (ex: \code{"classe"}).}

\item{sub_col}{Nom de la colonne contenant le sous-élément en format long (ex: \code{"molecule"}).}

\item{var_labels}{Vecteur nommé de libellés pour remplacer les noms des colonnes/groupes.}

\item{pct_type}{Type de pourcentage: \code{"total"} (par rapport au N total du jeu de données, défaut)
ou \code{"group"} (par rapport au total des éléments valides du groupe).}

\item{caption}{Titre/Légende du tableau.}

\item{digits}{Nombre de décimales pour l'affichage des pourcentages (défaut: 1).}

\item{color}{Couleur d'en-tête pour le thème analytique (défaut: \code{"transparent"}).}
}
\value{
Un objet \code{flextable} formaté avec lignes de groupes.
}
\description{
Génère un tableau récapitulatif (\code{flextable}) regroupant des sous-catégories/modalités
(ex: molécules) sous leurs catégories ou classes thématiques parentes (ex: classes thérapeutiques).
Supporte à la fois le format large (plusieurs colonnes) et le format long (deux colonnes groupe/élément).
}
\examples{
df <- data.frame(
  diuretique = c("Furosemide", "Indapamide", NA, "Furosemide"),
  iec        = c("Enalapril", NA, "Ramipril", "Enalapril"),
  ara2       = c(NA, "Valsartan", "Candesartan", NA)
)
desc_grouped(df, cols = c("diuretique", "iec", "ara2"),
  var_labels = c(diuretique = "Diurétique", iec = "IEC", ara2 = "ARA II"),
  caption = "Molécules prescrites par classe")

}
