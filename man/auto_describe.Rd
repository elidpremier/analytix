% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_describe.R
\name{auto_describe}
\alias{auto_describe}
\title{Analyse descriptive automatique de toutes les variables}
\usage{
auto_describe(
  data,
  vars = NULL,
  digits = 1,
  color = "#D3D3D3",
  binary_threshold = 2,
  cat_threshold = 10,
  verbose = FALSE
)
}
\arguments{
\item{data}{data.frame à analyser.}

\item{vars}{Vecteur de noms de colonnes à analyser. Si NULL (défaut), toutes les
colonnes sont analysées.}

\item{digits}{Nombre de décimales pour les statistiques (défaut: 1).}

\item{color}{Couleur d'en-tête des flextables (défaut: "#D3D3D3").}

\item{binary_threshold}{Nombre maximum de valeurs uniques pour qu'une variable
soit considérée binaire (défaut: 2).}

\item{cat_threshold}{Nombre maximum de valeurs uniques pour qu'une variable
soit considérée catégorielle (défaut: 10).}

\item{verbose}{Afficher les messages de progression (défaut: FALSE).}
}
\value{
Une liste nommée (par variable ou label) contenant :
\describe{
\item{chaque élément}{Résultat de la fonction descriptive correspondante (liste avec \verb{$flextable})}
\item{attr "var_types"}{data.frame avec colonnes \code{variable}, \code{label}, \code{type_detected}}
}
}
\description{
Détecte automatiquement le type de chaque variable (binaire, catégorielle,
numérique continue) et applique la fonction descriptive appropriée. Retourne une liste
structurée de résultats prête à être exportée vers Word.
}
\examples{
# Analyser toutes les variables d'iris
res <- auto_describe(iris)
names(res)  # variables détectées
attr(res, "var_types")  # tableau des types détectés

# Analyser seulement certaines variables
res2 <- auto_describe(iris, vars = c("Sepal.Length", "Species"))

}
