% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_names.R
\name{clean_names}
\alias{clean_names}
\title{Clean variable names to comply with Epi Info standards}
\usage{
clean_names(x, max_length = 64, prefix = "v_", sep = "_")
}
\arguments{
\item{x}{A character vector of variable names OR a data.frame whose column
names should be cleaned}

\item{max_length}{Maximum length of variable names (default: 64)}

\item{prefix}{Prefix to add if name doesn't start with a letter (default: "v_")}

\item{sep}{Separator used by \code{make.unique} for duplicate names (default: "_")}
}
\value{
If \code{x} is a character vector: a cleaned character vector.
If \code{x} is a data.frame: the same data.frame with cleaned column names.
}
\description{
This function transforms variable names to meet Epi Info requirements:
lowercase, no accents, underscores as separators, starts with a letter,
max length constraint, and unique names.
}
\examples{
# On a character vector
clean_names(c("Nom Patient", "Âge/Ans", "Température (°C)"))
# [1] "nom_patient"     "age_ou_ans"      "temperature_c"

# Directly on a data.frame
df <- data.frame("Nom Patient" = 1:3, "Âge/Ans" = 20:22)
clean_names(df)
}
